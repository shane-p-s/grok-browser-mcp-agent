# MCP (Streamable HTTP) + ASGI
mcp[cli]>=1.8.0,<2
starlette>=0.40.0
uvicorn[standard]>=0.30.0

# Browser agent
browser-use==0.12.6
playwright>=1.40.0

# HTTP
httpx>=0.27.0

# Encrypted local secrets store (Fernet)
cryptography>=43.0.0

# Config
python-dotenv>=1.0.0

# OAuth access tokens for /mcp (when OAUTH_ENABLED)
PyJWT>=2.8.0,<3
